### Changed
- Clarified RunPod endpoint defaults, scaling, and per-job policies in deployment and endpoint documentation.
- Split local Wan2.2, Cloud Wan2.2, and LTX-2.3 defaults in the generation modes documentation.

### Fixed
- Aligned frontend LTX-2.3 mode switching with the backend distilled defaults: 8 steps and cfg=1.0.
- Aligned the I2V frontend default CFG with the local Wan2.2 adapter default.
