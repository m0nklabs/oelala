interface:
  display_name: "Oelala"
  short_description: "Repo-specific patterns and workflows for oelala"
  default_prompt: "Use the oelala repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true