# ============================================
# OELALA - Tested GPU Requirements
# ============================================
# These are the exact versions tested and working
# with NVIDIA RTX 5090 / SM 1.20 / CUDA 12.8
#
# For other GPUs, you may use newer versions
# but these are guaranteed to work together.
# ============================================

# PyTorch 2.9.1 with CUDA 12.8 (SM 1.20 support)
# Install separately with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# Or for CUDA 12.4: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch==2.13.0
torchvision==0.28.0
torchaudio==2.11.0
torchsde==0.2.6

# Triton for GPU acceleration
triton==3.7.1

# Transformers & ML core
# GHSA-69w3-r845-3855 is patched in the first 5.0.0 release candidate.
transformers==5.14.1
accelerate==1.14.0
safetensors==0.8.0
sentencepiece==0.2.2

# GGUF / LLM support
gguf==0.19.0
llama-cpp-python==0.3.34

# Video processing
imageio==2.37.4
imageio-ffmpeg==0.6.0
opencv-python==4.13.0.92
opencv-python-headless==4.13.0.92

# Dynamic prompts
dynamicprompts==0.31.0

# Audio (for TTS nodes)
pyogg==0.6.14a1

# WebSocket and HTTP clients
httpx==0.28.1
websocket-client==1.9.0

# S3-compatible object storage (MinIO SDK)
minio==7.2.20

# YAML parsing (LoRA registry)
pyyaml==6.0.3
