# Supabase Auth Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# Supabase Project URL
SUPABASE_URL=https://xxxxx.supabase.co

# Supabase Keys (from Project Settings → API)
SUPABASE_ANON_KEY=eyJhbG...
SUPABASE_SERVICE_KEY=eyJhbG...

# JWT Secret (from Project Settings → API → JWT Settings)
SUPABASE_JWT_SECRET=your-jwt-secret

# oelala-storage Integration
# URL to the storage server (default: http://localhost:7990)
STORAGE_URL=http://localhost:7990
# API key for storage (create via /admin UI or config)
STORAGE_API_KEY=your-storage-api-key
# Signing secret for signed URLs (must match oelala-storage.yaml security.signing_secret)
# Generate with: openssl rand -hex 32
MEDIA_SIGNING_SECRET=your-signing-secret-here

# Stripe Payment Integration
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxx
# Webhook secret - get from Stripe CLI or Dashboard
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Frontend URL for Stripe redirects
FRONTEND_URL=http://localhost:5174

# Debug mode (0 or 1)
OELALA_DEBUG=0

# Guardian LLM Proxy - VRAM Management
# Unloads LLM before ComfyUI generation to free all VRAM for media generation.
# Guardian auto-reloads the model on the next inference request (~30s warm-up).
GUARDIAN_BASE_URL=http://192.168.1.2:11434
GUARDIAN_API_KEY=your-guardian-api-key

# Google OAuth (configured in Supabase dashboard, not needed here)
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-xxx
